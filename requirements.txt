# Use the CPU-only PyTorch build (much smaller than the default CUDA build,
# which is too large for Streamlit Community Cloud).
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.30
torch>=2.0
mido>=1.3
